#include "catch.hpp"
#include "Card.h"

/*
typedef struct {int player; int score;} PokRes;

class SokoGame {

public:
        static const int INCLUDE_ALL_CARDS = 0;
        static const int EXCLUDE_FIRST_CARD = 1;
    	SokoGame();
    	virtual ~SokoGame();
        void play();
        void testHands();
        void autoPlay(int);
private:
        //int roundNr;

       	void initPlayers(std::vector<Player*>& players) const;
        void deal(std::vector<Player*>& players, CardDeck& deck, int count, int round) const;
        PokRes findBestHand(const std::vector<Player*>&, int startIndex) const;
        void showResultsOfRound(std::vector<Player*>&, int potti, int round) const;
        void dispAllCards(const std::vector<Player*>&, int round) const;
        void dispHands(const std::vector<Player*>& pl) const;
        int takeBets(std::vector<Player*>&, int cardNr, int pot) const;
        int forceBet(std::vector<Player*>&, int bet) const;
        void resetPlayerStatusAndCards(std::vector<Player*>& pl) const;
        int activePlayers(const std::vector<Player*>& pl) const;
        bool checkBets(const std::vector<Player*>& pl, const std::vector<int>& bets) const;
        int getMinBet(const std::vector<Player*>& pl, unsigned int plIndex, const std::vector<int>& bets) const;
        int getMaxBet(const std::vector<Player*>& pl, const std::vector<int>& bets) const;
        int highestScoreId(std::vector<PokerHand>&) const;
};

*/
